apiVersion: v1
kind: Service
metadata:
  name: odrl-ui
  namespace: odrl
  labels:
    app.kubernetes.io/name: odrl-ui
    app.kubernetes.io/part-of: odrl-translator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: odrl-ui
  ports:
    - name: http
      port: 7860
      targetPort: http